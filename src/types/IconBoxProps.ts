import { ReactNode } from "react";
import { ClassElement } from "typescript";

export interface IconBoxProps {
    data: number;
    icon: ReactNode;
    iconClasses: string;
    textClasses: string;
}
